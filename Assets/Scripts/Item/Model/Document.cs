public class Document : ItemEventBasic
{
    // Start is called before the first frame update
    void Start()
    {
        this.gameObject.name = ItemNameDictionary.Document;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
