public class Hand : ItemEventBasic
{
    // Start is called before the first frame update
    void Start()
    {
        this.gameObject.name = ItemNameDictionary.Hand;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
